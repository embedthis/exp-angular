{
    "name": "exp-ng",
    "description": "Angular Tooling Plugin for Expansive",
    "version": "0.1.0",
    "keywords": [
        "expansive-plugin",
        "ng"
    ],
    "licenses": "GPL",
    "scripts": {
        "postcache": {
            "script": "Cmd.locate('uglifyjs') || run('npm install -g uglifyjs') ; Cmd.locate('ng-annotate') || run('npm install -g ng-annotate')"
        }
    },
    "install": false
}
